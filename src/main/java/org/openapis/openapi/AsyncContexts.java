/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package org.openapis.openapi;

import static org.openapis.openapi.operations.Operations.AsyncRequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import org.openapis.openapi.models.components.CreateContextRequestDto;
import org.openapis.openapi.models.components.UpdateContextRequestDto;
import org.openapis.openapi.models.operations.ContextsControllerCreateContextRequest;
import org.openapis.openapi.models.operations.ContextsControllerDeleteContextRequest;
import org.openapis.openapi.models.operations.ContextsControllerGetContextRequest;
import org.openapis.openapi.models.operations.ContextsControllerListContextsRequest;
import org.openapis.openapi.models.operations.ContextsControllerUpdateContextRequest;
import org.openapis.openapi.models.operations.async.ContextsControllerCreateContextRequestBuilder;
import org.openapis.openapi.models.operations.async.ContextsControllerCreateContextResponse;
import org.openapis.openapi.models.operations.async.ContextsControllerDeleteContextRequestBuilder;
import org.openapis.openapi.models.operations.async.ContextsControllerDeleteContextResponse;
import org.openapis.openapi.models.operations.async.ContextsControllerGetContextRequestBuilder;
import org.openapis.openapi.models.operations.async.ContextsControllerGetContextResponse;
import org.openapis.openapi.models.operations.async.ContextsControllerListContextsRequestBuilder;
import org.openapis.openapi.models.operations.async.ContextsControllerListContextsResponse;
import org.openapis.openapi.models.operations.async.ContextsControllerUpdateContextRequestBuilder;
import org.openapis.openapi.models.operations.async.ContextsControllerUpdateContextResponse;
import org.openapis.openapi.operations.ContextsControllerCreateContext;
import org.openapis.openapi.operations.ContextsControllerDeleteContext;
import org.openapis.openapi.operations.ContextsControllerGetContext;
import org.openapis.openapi.operations.ContextsControllerListContexts;
import org.openapis.openapi.operations.ContextsControllerUpdateContext;
import org.openapis.openapi.utils.Headers;
import org.openapis.openapi.utils.Options;


public class AsyncContexts {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final Contexts syncSDK;

    AsyncContexts(Contexts syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public Contexts sync() {
        return syncSDK;
    }


    /**
     * Create a context
     * 
     * <p>Create a new context with the specified type, id, and data. Returns 409 if context already exists.
     * 
     * <p>**type** and **id** are required fields, **data** is optional, if the context already exists, it
     * returns the 409 response
     * 
     * @return The async call builder
     */
    public ContextsControllerCreateContextRequestBuilder create() {
        return new ContextsControllerCreateContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a context
     * 
     * <p>Create a new context with the specified type, id, and data. Returns 409 if context already exists.
     * 
     * <p>**type** and **id** are required fields, **data** is optional, if the context already exists, it
     * returns the 409 response
     * 
     * @param body 
     * @return {@code CompletableFuture<ContextsControllerCreateContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerCreateContextResponse> create(@Nonnull CreateContextRequestDto body) {
        return create(null, body, null);
    }

    /**
     * Create a context
     * 
     * <p>Create a new context with the specified type, id, and data. Returns 409 if context already exists.
     * 
     * <p>**type** and **id** are required fields, **data** is optional, if the context already exists, it
     * returns the 409 response
     * 
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<ContextsControllerCreateContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerCreateContextResponse> create(
            @Nullable String idempotencyKey, @Nonnull CreateContextRequestDto body,
            @Nullable Options options) {
        ContextsControllerCreateContextRequest request = new ContextsControllerCreateContextRequest(idempotencyKey, body);
        AsyncRequestOperation<ContextsControllerCreateContextRequest, ContextsControllerCreateContextResponse> operation
              = new ContextsControllerCreateContext.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * List all contexts
     * 
     * <p>Retrieve a paginated list of all contexts, optionally filtered by type and key pattern.
     * 
     * <p>**type** and **id** are optional fields, if provided, only contexts with the matching type and id
     * will be returned.
     * **search** is an optional field, if provided, only contexts with the matching key pattern will be
     * returned.
     * Checkout all possible parameters in the query section below for more details
     * 
     * @return The async call builder
     */
    public ContextsControllerListContextsRequestBuilder list() {
        return new ContextsControllerListContextsRequestBuilder(sdkConfiguration);
    }

    /**
     * List all contexts
     * 
     * <p>Retrieve a paginated list of all contexts, optionally filtered by type and key pattern.
     * 
     * <p>**type** and **id** are optional fields, if provided, only contexts with the matching type and id
     * will be returned.
     * **search** is an optional field, if provided, only contexts with the matching key pattern will be
     * returned.
     * Checkout all possible parameters in the query section below for more details
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return {@code CompletableFuture<ContextsControllerListContextsResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerListContextsResponse> list(@Nonnull ContextsControllerListContextsRequest request) {
        return list(request, null);
    }

    /**
     * List all contexts
     * 
     * <p>Retrieve a paginated list of all contexts, optionally filtered by type and key pattern.
     * 
     * <p>**type** and **id** are optional fields, if provided, only contexts with the matching type and id
     * will be returned.
     * **search** is an optional field, if provided, only contexts with the matching key pattern will be
     * returned.
     * Checkout all possible parameters in the query section below for more details
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return {@code CompletableFuture<ContextsControllerListContextsResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerListContextsResponse> list(@Nonnull ContextsControllerListContextsRequest request, @Nullable Options options) {
        AsyncRequestOperation<ContextsControllerListContextsRequest, ContextsControllerListContextsResponse> operation
              = new ContextsControllerListContexts.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Update a context
     * 
     * <p>Update the data of an existing context.
     * **type** and **id** are required fields, **data** is required. Only the data field is updated, the
     * rest of the context is not affected.
     * If the context does not exist, it returns the 404 response
     * 
     * @return The async call builder
     */
    public ContextsControllerUpdateContextRequestBuilder update() {
        return new ContextsControllerUpdateContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Update a context
     * 
     * <p>Update the data of an existing context.
     * **type** and **id** are required fields, **data** is required. Only the data field is updated, the
     * rest of the context is not affected.
     * If the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param body 
     * @return {@code CompletableFuture<ContextsControllerUpdateContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerUpdateContextResponse> update(
            @Nonnull String id, @Nonnull String type,
            @Nonnull UpdateContextRequestDto body) {
        return update(
                id, type, null,
                body, null);
    }

    /**
     * Update a context
     * 
     * <p>Update the data of an existing context.
     * **type** and **id** are required fields, **data** is required. Only the data field is updated, the
     * rest of the context is not affected.
     * If the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param idempotencyKey A header for idempotency purposes
     * @param body 
     * @param options additional options
     * @return {@code CompletableFuture<ContextsControllerUpdateContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerUpdateContextResponse> update(
            @Nonnull String id, @Nonnull String type,
            @Nullable String idempotencyKey, @Nonnull UpdateContextRequestDto body,
            @Nullable Options options) {
        ContextsControllerUpdateContextRequest request = new ContextsControllerUpdateContextRequest(
                id, type, idempotencyKey,
                body);
        AsyncRequestOperation<ContextsControllerUpdateContextRequest, ContextsControllerUpdateContextResponse> operation
              = new ContextsControllerUpdateContext.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Retrieve a context
     * 
     * <p>Retrieve a specific context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @return The async call builder
     */
    public ContextsControllerGetContextRequestBuilder get() {
        return new ContextsControllerGetContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve a context
     * 
     * <p>Retrieve a specific context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @return {@code CompletableFuture<ContextsControllerGetContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerGetContextResponse> get(@Nonnull String id, @Nonnull String type) {
        return get(
                id, type, null,
                null);
    }

    /**
     * Retrieve a context
     * 
     * <p>Retrieve a specific context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param idempotencyKey A header for idempotency purposes
     * @param options additional options
     * @return {@code CompletableFuture<ContextsControllerGetContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerGetContextResponse> get(
            @Nonnull String id, @Nonnull String type,
            @Nullable String idempotencyKey, @Nullable Options options) {
        ContextsControllerGetContextRequest request = new ContextsControllerGetContextRequest(id, type, idempotencyKey);
        AsyncRequestOperation<ContextsControllerGetContextRequest, ContextsControllerGetContextResponse> operation
              = new ContextsControllerGetContext.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Delete a context
     * 
     * <p>Delete a context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @return The async call builder
     */
    public ContextsControllerDeleteContextRequestBuilder delete() {
        return new ContextsControllerDeleteContextRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a context
     * 
     * <p>Delete a context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @return {@code CompletableFuture<ContextsControllerDeleteContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerDeleteContextResponse> delete(@Nonnull String id, @Nonnull String type) {
        return delete(
                id, type, null,
                null);
    }

    /**
     * Delete a context
     * 
     * <p>Delete a context by its type and id.
     * **type** and **id** are required fields, if the context does not exist, it returns the 404 response
     * 
     * @param id Context ID
     * @param type Context type
     * @param idempotencyKey A header for idempotency purposes
     * @param options additional options
     * @return {@code CompletableFuture<ContextsControllerDeleteContextResponse>} - The async response
     */
    public CompletableFuture<ContextsControllerDeleteContextResponse> delete(
            @Nonnull String id, @Nonnull String type,
            @Nullable String idempotencyKey, @Nullable Options options) {
        ContextsControllerDeleteContextRequest request = new ContextsControllerDeleteContextRequest(id, type, idempotencyKey);
        AsyncRequestOperation<ContextsControllerDeleteContextRequest, ContextsControllerDeleteContextResponse> operation
              = new ContextsControllerDeleteContext.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
